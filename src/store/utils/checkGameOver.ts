export {}
// import { IContainer } from '../types/types';

// export const checkGameOver = (state: IContainer) => {
//   const { rows, columns, field } = state;
//   for (let i = 0; i < rows; i++) {
//     for (let j = 0; j < columns; j++) {
//       if (field[i][j].value === 0) return false;
//     }
//   }
//   return true;
// };
